#include <stdio.h>

int main(void)
{
	// int fd1;
	// int fd2;
	// char c;
	// fd = open("foo.txt", O_RDONLY);
	// if ( fd1 == -1){
		// write(2, "File failed to open in read mode\n", 33);
		// exit(1);
	// }

	printf("%X\n", 63213);
	return 0;
}
