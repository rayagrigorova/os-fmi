int main(int argc, char* argv[]) {
	// Algorithm: 
	// while read word1, word2
		 // cmd (w1, w2);

	// char byte;
	// char* args[5]; // the two arguments for the command 
	// int curr_char = 0;
	// int curr_word = 0;
	// while read byte: 
		// if curr_char > 4:
			// errx()
		// if byte == delimeter:
			// args[curr_word][curr_char] = '\0'
			// curr_word++;
			// if (curr_word == 2):
				//	execute the command with args[0] and args[1]
				//  curr_word = 0
			// curr_char = 0
		// else:
			// args[curr_word][curr_char++] = byte;
		// endwhile
		
		// check the last byte that was read 
		// args[curr_word][curr_char] = '\0';
		// This should include a while loop for 
		// skipping delimeters 
		// if b is delimeter: 
			// if curr_word == 1 
				// cmd(args[0]);
		// else: 
			// if curr_word == 0
	// 			cmd(args[0]);
			// else:
	// 			cmd(args[1]);

			
}
